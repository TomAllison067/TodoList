<%- include("header") -%>

<div class="box" id="heading">
    <h1><%= today %></h1>
</div>
<div class="box">
    <% items.forEach(item => { %>

    <form action='/delete' method='POST'>
        <div class="item">
            <input type="checkbox" onclick="this.form.submit()" name="checkbox" value="<%= item.id %>">
            <p><%= item.body %></p>
        </div>
    </form>

    <% }); %>

    <form class="item" id="add" action="<%= formAction %>" method="POST">
        <input type="text" name="itemBody" placeholder="Add to list.." autocomplete="off"></input>
        <button type="submit" name="list" value="<%= list %>">+</button>
    </form>
</div>

<%- include("footer") -%>