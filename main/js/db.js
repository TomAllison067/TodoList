const mongoose = require('mongoose');

mongoose.connect("mongodb://localhost:27017/todoDB", {useNewUrlParser: true});
module.exports = {
    module = []

    module.exports = {
        mongoose;
    }
}